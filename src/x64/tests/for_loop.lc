main :: proc(): int {
    a := 0;
    for i := 0; i < 10; i += 1 {
        a += 1;
    }

    for a > 0; a -= 1 {
        c := 20;
        c += 1;
    }

    a = 10;
    for a > 0 {
        a -= 1;
    }

    return a;
}